<?php
function Matrix()
{
  echo "\nWake up neo...\n";
  readline("> ");

  echo "\nThe Matrix has you...\n";
  readline("> ");

  echo "\nFollow the white rabbit.\n";
}
Matrix();


// > start program with "wake up neo", delay each word in the future

// > give user ability to answer, but then the machine should respond with
// "the matrix has got you" regardless of what the user answered with

// > add some conditionality to how the machine reacts later
// > delay the way the text is displayed on the terminal, sort of like the scene in the movie, similar to a typerwriting animation
